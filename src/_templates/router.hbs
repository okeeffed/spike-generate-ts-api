import { initContract } from "@ts-rest/core";
import { z } from "zod";

const c = initContract();

{{#each refs}}
const {{{@key}}} = {{{this}}}
{{/each}}

export const contract = c.router({
  {{#each endpoints}}
  {{> endpoint this}},
  {{/each}}
});
